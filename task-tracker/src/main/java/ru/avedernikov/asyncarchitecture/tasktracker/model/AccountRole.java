package ru.avedernikov.asyncarchitecture.tasktracker.model;

// todo: lowercase enums do not match conventions but it is faster
public enum AccountRole {
    worker,
    manager,
    admin,
    lead
}
